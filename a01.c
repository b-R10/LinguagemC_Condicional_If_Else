/*
    d) Fazer uma solução estruturando as condições com base no questionário a seguir.
    Sexo feminino e idade maior que 10?
    Já engravidou?
    Se tem filhos, quantos tem e informar a idade do mais velho.
    Exibir o dados informados.
*/

#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <string.h>
#include <ctype.h>

int main() {
    printf("Escolha um desses dois sexos: masculino / feminino\n");
    scanf("%s", %sexo);
    printf("digite a idade: \n");
    scanf("%i", &idade);
    if ((strcasecmp(sexo, "feminino") == 0 || strcasecmp(sexo, "f") == 0) &&
         strcasecmp(sexo,))
    return 0;
}